import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import type { practiceAreas } from "@/lib/site";
type Area = (typeof practiceAreas)[number];
export function PracticeCard({ area }: { area: Area }) { const Icon = area.icon; return <Link href={"/practice-areas/" + area.slug} className="group min-w-[284px] overflow-hidden rounded-sm border border-gold/20 bg-midnight/72 shadow-velvet transition duration-300 hover:-translate-y-1 hover:border-gold/70 hover:shadow-gold md:min-w-0"><div className="relative h-44"><img src={area.image} alt="" className="h-full w-full object-cover opacity-85 transition duration-300 group-hover:scale-105"/><div className="absolute inset-0 bg-gradient-to-t from-obsidian via-obsidian/35 to-transparent"/><div className="absolute left-5 top-5 flex h-12 w-12 items-center justify-center rounded-sm border border-gold/35 bg-black/60 text-gold"><Icon size={24}/></div></div><div className="p-5"><div className="flex items-start justify-between gap-4"><h3 className="font-display text-2xl text-ivory">{area.title}</h3><ArrowUpRight className="mt-1 shrink-0 text-gold" size={20}/></div><p className="mt-3 text-sm leading-7 text-body">{area.short}</p></div></Link>; }
