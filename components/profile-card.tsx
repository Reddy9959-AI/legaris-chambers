import type { Person } from "@/lib/people";
function initials(name: string) { return name.split(" ").filter(Boolean).map((part) => part[0]).join("").slice(0, 3); }
export function ProfileCard({ person }: { person: Person }) { return <article className="group flex h-full flex-col overflow-hidden rounded-sm border border-gold/25 bg-[linear-gradient(180deg,rgba(17,24,39,.88),rgba(6,6,6,.96))] shadow-velvet transition duration-300 hover:-translate-y-1 hover:border-gold/70 hover:shadow-gold"><div className="relative flex min-h-64 items-center justify-center overflow-hidden border-b border-gold/20 bg-black patterned"><div className="absolute inset-0 bg-[radial-gradient(circle_at_50%_20%,rgba(212,175,55,.22),transparent_40%)]"/><div className="relative flex h-32 w-32 items-center justify-center rounded-sm border border-gold/50 bg-obsidian/90 font-display text-5xl text-gold shadow-gold">{initials(person.name)}</div><div className="absolute bottom-4 left-4 right-4 border-l border-gold/60 pl-4"><p className="text-xs font-semibold uppercase tracking-[0.22em] text-gold">{person.section}</p></div></div><div className="flex flex-1 flex-col p-6"><h3 className="font-display text-3xl leading-tight text-ivory">{person.name}</h3><p className="mt-3 min-h-12 text-sm font-semibold uppercase tracking-[0.14em] text-gold">{person.role}</p><div className="mt-6 border-t border-gold/15 pt-5"><h4 className="text-sm font-semibold uppercase tracking-[0.16em] text-ivory">Responsibilities</h4><ul className="mt-4 grid gap-3 text-sm leading-6 text-body">{person.responsibilities.map((item) => <li key={item} className="flex gap-3"><span className="mt-2 h-1.5 w-1.5 shrink-0 rounded-full bg-gold"/><span>{item}</span></li>)}</ul></div></div></article>; }
